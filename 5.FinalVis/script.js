
document.addEventListener("DOMContentLoaded", () => {

    getData();
});

function setup(data) {
    // Sets up the persistent pieces of the visualization, e.g. scales, groups, axes

    update();
}


function update() {
    // Draws the shapes
}

function getData() {
    // gets data and calls setup() when it is has the data

}



